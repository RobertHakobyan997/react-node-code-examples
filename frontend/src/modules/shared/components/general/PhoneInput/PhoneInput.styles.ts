import { createUseStyles } from 'react-jss'
import { AppTheme } from 'src/modules/shared/types/style.types'

export const phoneInputStyles = createUseStyles((theme: AppTheme) => ({
  PhoneInputOptionsWrapper: {
    width: '91px',
  },
}))
