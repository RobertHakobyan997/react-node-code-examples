import { createUseStyles } from 'react-jss'

export const errorPageUseStyles = createUseStyles(() => ({
  wrapper: {
    height: 'var(--screen-size)',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
}))
