import { createUseStyles } from 'react-jss'
import { AppTheme } from '../../../shared/types/style.types'

export const accordionWrapperUseStyles = createUseStyles((theme: AppTheme) => ({
  accordionWrapper: {
    margin: '1.25rem auto',
  },
}))
