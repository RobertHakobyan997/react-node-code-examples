export const DEFAULT_PAGE_SIZE = 10;

export const EXCEPTIONS = {
  INVALID_ID                       : 'Invalid ID format',
  INTERNAL_SERVER_ERROR            : 'Internal Server Error',
  FORBIDDEN                        : 'Forbidden',
  LOCAL_DB_FILES_DIR_NOT_CONFIGURED: 'Local DB files dir is not configured',
  COLLECTIONS_LIST_NOT_CONFIGURED  : 'Collections list is not configured',
};
